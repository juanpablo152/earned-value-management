services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: earned-value-management-frontend
    ports:
      - "4200:4200"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development
      - NG_CLI_ANALYTICS=false
    stdin_open: true
    tty: true

